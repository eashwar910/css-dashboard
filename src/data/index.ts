// Barrel export for all mock data modules.
// Import from here inside hooks; do NOT import individual data files directly
// from UI components — always go through the hooks.
export { mockTasks } from './tasks';
